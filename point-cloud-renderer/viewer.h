class Viewer {
    public:
        Camera camera;
        
};

// Defines the view 
class Camera {

};

// Point Cloud Graphics Object
class PointCloud {
    public:
        PointCloud() {
            
        }

};

// 3D Object
class Object3D {
    public:
        Object3D() {
            // The VAO (vertex attribute array) describes the memory layout
            // of vertex data in a way shaders (which run on the GPU) can utilize 
            
            // Buffers are GPU memory which verticies will be loaded too

            //
        }

        void update() {

        }
    
    private:
        // State variables
        

};

